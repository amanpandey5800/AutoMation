Working directory,local repository,remote repository

working directory- its just like a windows directory any file in this not part of remote repository.
local repository- 


git init

git config --global user.nam = "name"
git config --global user.name="123@gmail.ocm"



commands
============
1.git init = converts an existing directory into gihub directory

gir log stores what commit has been done;
